name: my_quran_app
description: A new Flutter project.
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # پکیج‌های حیاتی (بدون این‌ها game_state خطا می‌دهد)
  provider: ^6.0.5
  just_audio: ^0.9.36
  flutter_cache_manager: ^3.3.1
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.7
  animate_do: ^3.1.0
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/
    - assets/data/lessons/
    - assets/audio/